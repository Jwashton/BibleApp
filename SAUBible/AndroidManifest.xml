<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.southern" 
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name="BibleApp">
        <activity
            android:name="edu.southern.HomeScreen"  
            android:label="@string/app_name" 
            android:theme="@style/BibleTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="edu.southern.BibleNavigation" 
            android:label="@string/title_activity_bible_navigation"
            android:theme="@style/BibleTheme" >
        </activity>
    </application>

</manifest>